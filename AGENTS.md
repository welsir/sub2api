# Sub2API 仓库开发约束

## 适用范围

- 本文件是 Sub2API 仓库的项目级开发约束，适用于三个定制业务分支。
- 同一个代码仓库中的不同分支代表不同产品线，不代表同一功能的不同发布阶段。
- 每次分析、开发、修复或提交前，必须先运行 `git branch --show-current` 和 `git status --short --branch`，确认当前分支及已有改动。
- 如果处于 detached HEAD、未知分支，或者任务没有明确目标分支，先停止修改并确认归属。

## 分支职责与目标名称

| 产品线 | 当前分支名称 | 未来目标名称 | 职责 |
| --- | --- | --- | --- |
| 上游基线 | `main` | `main` | 永远指向最新的 `upstream/main`，保持纯净，不承载任何定制业务改动。 |
| 公司业务 | `dev/company-01-runtime-merge` | `dev/company` | 承载公司自己的业务逻辑、公司场景需求及对应适配。 |
| Omni 用户版 | `dev/omni` | `dev/omni` | 面向外部用户提供使用，承载用户侧适配、兼容和产品定制。 |
| 团队内部版 | `dev/team` | `dev/team` | 面向自己的团队内部使用，承载内部工作流、内部能力和团队场景适配。 |

- `dev/omni` 和 `dev/team` 已经是实际分支名称；旧名称 `codex/omni-latest-selected-customs` 和 `tml/team-codex-compat` 已停用，不得继续作为开发目标或被重新创建。
- `dev/company` 仍只是已经确认的未来目标名称。在用户再次明确下达公司分支改名指令之前，不得创建 `dev/company`，不得重命名或删除 `dev/company-01-runtime-merge`，不得修改其远端跟踪关系，也不得移动其 worktree。
- 上表“当前分支名称”是执行任务时的真实名称。

## 分支隔离原则

- 所有业务改动默认只属于产生该改动的当前分支。
- 不得因为某项功能“看起来通用”“在另一个分支也有用”或“修复方式相同”，就推断它应该同步到其他分支。
- 未经用户明确指定来源分支、目标分支和同步范围，不得在这些分支之间执行 merge、rebase、cherry-pick、patch 搬运、文件复制、回合并、前向移植或反向移植。
- 不得把任何公司、Omni 或团队业务改动合入 `main`。
- 在分支 A 完成功能或修复后，不得顺手把它同步到分支 B、分支 C 或 `main`。
- 同一需求如果确实需要在多个业务分支实现，必须视为多个独立交付：分别确认范围、分别实现、分别测试、分别验收。
- 如果用户只描述了功能而没有说明目标业务线，应结合当前分支和上下文判断；仍存在歧义时，必须先询问，不得自行选择或扩散到多个分支。

## `main` 纯净规则

- `upstream/main` 是 `main` 的唯一上游事实来源；不要默认把 `origin/main` 当作纯净上游基线。
- 本地 `main` 应与最新的 `upstream/main` 保持一致，并保持工作区干净。
- `main` 只允许执行用于获取、核对或保持与 `upstream/main` 一致的操作。
- 不得在 `main` 上开发功能、修复定制问题、添加定制配置、提交定制文档或接受任一业务分支的合并。
- 为保持零定制改动，本仓库级 `AGENTS.md` 不提交到 `main`。
- 如果发现 `main` 与 `upstream/main` 不一致，停止在 `main` 上继续开发，先报告差异并确认处理方式。

## 各业务线边界

### 公司业务分支

- 仅围绕公司业务目标和公司环境进行开发。
- 不得默认引入 Omni 面向外部用户的适配，也不得默认引入团队内部专用能力。

### Omni 用户版分支

- 以外部用户可用性、兼容性和用户侧产品体验为边界。
- 不得默认引入公司专属业务规则，也不得默认暴露团队内部能力。

### 团队内部版分支

- 以内部团队工作方式、内部工具和内部运行需求为边界。
- 不得把团队内部实现默认视为适合外部用户或公司业务的通用功能。

## 跨分支工作的授权要求

- 只有用户明确要求跨分支处理时，才能开始跨分支工作。
- 执行前必须列明来源分支、目标分支、允许搬运的提交或文件，以及明确排除的内容。
- 执行时应使用独立 worktree 或其他清晰隔离方式，避免共享工作区污染。
- 执行后必须分别验证每个目标分支，并报告实际进入各分支的文件和提交。
- 一次跨分支授权只适用于当次指定范围，不构成以后自动同步的长期授权。

## 治理文件例外

- 这份 `AGENTS.md` 可以在三个定制业务分支中保持相同，作为明确的仓库治理例外。
- 该例外只适用于分支职责和开发约束等治理内容，不得扩展解释为业务代码、配置、迁移、接口、前端或部署文件可以互相同步。
- 后续修改本文件时，也应确认变更仅涉及治理规则，并核对三个业务分支中的版本是否符合用户当次要求。

## 交付检查

- 开始工作时说明当前分支及其产品线职责。
- 保留并避开工作区中已有的用户改动，不得把无关文件带入当前任务。
- 完成前检查 `git diff --name-only` 和 `git status --short`，确认改动只落在目标分支及授权范围内。
- 交付说明必须写清：实际修改的分支、修改文件、验证结果，以及哪些分支明确没有同步。
